services:
  airs:
    build:
      context: ./airs
      args:
        - AIRS_BASE_IMAGE=${AIRS_BASE_IMAGE}
    image: ${AIRS_TAG}
    container_name: airs
    ports:
      - "8080:8080"
    volumes:
      - ./work:/work
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all