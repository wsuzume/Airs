services:
  airs:
    build:
      context: ./airs
      args:
        - AIRS_BASE_IMAGE=${AIRS_BASE_IMAGE}
    image: ${AIRS_TAG}
    container_name: airs
    ports:
      - "8080:8080"
    runtime: nvidia
    volumes:
      - ./work:/work
    environment:
      - NVIDIA_VISIBLE_DEVICES=all